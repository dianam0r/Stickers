---
const { categories } = Astro.props;
---

<ul>
    {
        categories.map((category) => (
            <li>
                <a href={`/categories/${category.slug}`}>{category.name}</a>
            </li>
        ))
    }
</ul>
